# GA4 + Consent-Gating Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Google Analytics 4 (Consent Mode v2) auf wohlstandsmarketing.de einbinden und den bestehenden, funktionslosen Cookie-Banner so scharfschalten, dass er GA4, PostHog und Clarity DSGVO-konform steuert.

**Architecture:** Ein zentrales Consent-Modul (`lib/consent.ts`) ist Single Source of Truth (localStorage + Pub/Sub über `CustomEvent`). GA4 nutzt Consent Mode v2 „advanced" (Default `denied` → cookielose Pings; `update` auf `granted` nach Einwilligung). PostHog und Clarity laden erst nach Einwilligung. Eine gemeinsame Routen-Liste (`lib/tracking-routes.ts`) hält interne Seiten DRY.

**Tech Stack:** Next.js 16.2.6 (App Router), React 19, TypeScript, `next/script`, posthog-js, Microsoft Clarity, Google gtag.js.

## Global Constraints

- **Next.js 16.2.6** — APIs weichen ggf. von älteren Versionen ab. Vor Code-Änderungen die relevante Doku unter `node_modules/next/dist/docs/01-app/` lesen. `next/script` ist bestätigt verfügbar.
- **Kein automatisierter Testrunner** im Projekt (kein jest/vitest). Verifikation pro Task = `npm run lint` + `npm run build` + manuelle Browser-Prüfung (konkret je Task beschrieben).
- **Sprache:** UI-Texte Deutsch mit echten Umlauten (ä/ö/ü/ß). Kommentare Deutsch, Code/Bezeichner Englisch.
- **Measurement-ID:** `G-Z8TF8WM9MF`, ausschließlich über `process.env.NEXT_PUBLIC_GA_ID` referenzieren — nie hartkodiert.
- **Bestehender localStorage-Key beibehalten:** `wsm-cookie-consent`, Format `{ decision: "accept" | "decline", at: ISOString }`.
- **Interne Routen** (kein öffentliches Tracking): `/tools`, `/rechner`, `/outreach`, `/analytics`, `/finanzen`, `/angebot`.
- **Risiko-Entscheidung (dokumentiert):** GA4 sendet ohne Einwilligung cookielose Pings (Consent Mode `denied`). Bewusst gewählt (Spec 2026-06-23, Option B). PostHog/Clarity laden NICHT ohne Einwilligung.
- **Commits:** klein und häufig, eine Aufgabe = ein Commit. Conventional-Commit-Präfixe (`feat:`, `refactor:`, `docs:`).

---

### Task 1: Consent-Kern + gemeinsame Routen-Liste

**Files:**
- Create: `lib/tracking-routes.ts`
- Create: `lib/consent.ts`

**Interfaces:**
- Produces:
  - `BARE_PREFIXES: readonly string[]`
  - `isInternalRoute(path: string | null | undefined): boolean`
  - `type ConsentDecision = "accept" | "decline"`
  - `getConsent(): ConsentDecision | null`
  - `setConsent(decision: ConsentDecision): void`
  - `subscribeConsent(cb: (d: ConsentDecision) => void): () => void`
  - `openCookieSettings(): void`
  - `subscribeOpenSettings(cb: () => void): () => void`

- [ ] **Step 1: `lib/tracking-routes.ts` erstellen**

```ts
/**
 * Interne, passwortgeschützte Routen, auf denen KEIN öffentliches Tracking
 * läuft. Single Source of Truth für GlobalOverlays, PostHog und GA4 (DRY).
 */
export const BARE_PREFIXES = [
  "/tools",
  "/rechner",
  "/outreach",
  "/analytics",
  "/finanzen",
  "/angebot",
] as const;

export function isInternalRoute(path: string | null | undefined): boolean {
  return !!path && BARE_PREFIXES.some((p) => path.startsWith(p));
}
```

- [ ] **Step 2: `lib/consent.ts` erstellen**

```ts
/**
 * Zentrale Cookie-/Consent-Verwaltung (Single Source of Truth).
 * - Speichert die Entscheidung im bestehenden Key `wsm-cookie-consent`.
 * - Meldet Änderungen via CustomEvent an alle Tracking-Tools (kein Reload nötig).
 * - `openCookieSettings()` blendet den Banner zum Widerruf erneut ein.
 */
export type ConsentDecision = "accept" | "decline";

const STORAGE_KEY = "wsm-cookie-consent";
const CHANGE_EVENT = "wsm-consent-change";
const OPEN_EVENT = "wsm-open-cookie-settings";

export function getConsent(): ConsentDecision | null {
  if (typeof window === "undefined") return null;
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw) as { decision?: unknown };
    return parsed?.decision === "accept" || parsed?.decision === "decline"
      ? (parsed.decision as ConsentDecision)
      : null;
  } catch {
    return null;
  }
}

export function setConsent(decision: ConsentDecision): void {
  if (typeof window === "undefined") return;
  try {
    localStorage.setItem(
      STORAGE_KEY,
      JSON.stringify({ decision, at: new Date().toISOString() })
    );
  } catch {
    /* localStorage blockiert → trotzdem Event feuern, damit die Session reagiert */
  }
  window.dispatchEvent(new CustomEvent<ConsentDecision>(CHANGE_EVENT, { detail: decision }));
}

export function subscribeConsent(cb: (d: ConsentDecision) => void): () => void {
  if (typeof window === "undefined") return () => {};
  const handler = (e: Event) => cb((e as CustomEvent<ConsentDecision>).detail);
  window.addEventListener(CHANGE_EVENT, handler);
  return () => window.removeEventListener(CHANGE_EVENT, handler);
}

export function openCookieSettings(): void {
  if (typeof window === "undefined") return;
  window.dispatchEvent(new Event(OPEN_EVENT));
}

export function subscribeOpenSettings(cb: () => void): () => void {
  if (typeof window === "undefined") return () => {};
  const handler = () => cb();
  window.addEventListener(OPEN_EVENT, handler);
  return () => window.removeEventListener(OPEN_EVENT, handler);
}
```

- [ ] **Step 3: Lint + Build prüfen**

Run: `npm run lint && npm run build`
Expected: Kein Fehler, kein Type-Error in den neuen Dateien.

- [ ] **Step 4: Commit**

```bash
git add lib/tracking-routes.ts lib/consent.ts
git commit -m "feat: zentrales Consent-Modul + gemeinsame Tracking-Routen"
```

---

### Task 2: GA4-Komponente + Env + Layout-Einbindung

**Files:**
- Modify: `.env.local` (Variable ergänzen)
- Create: `components/GoogleAnalytics.tsx`
- Modify: `app/layout.tsx` (Import + Mount)

**Interfaces:**
- Consumes: `isInternalRoute` (Task 1), `getConsent`, `subscribeConsent` (Task 1)
- Produces: `default export GoogleAnalytics()` (React-Client-Komponente, no-op ohne `NEXT_PUBLIC_GA_ID`)

- [ ] **Step 1: `.env.local` ergänzen**

Zeile hinzufügen (vorhandene Variablen nicht anfassen):

```
NEXT_PUBLIC_GA_ID=G-Z8TF8WM9MF
```

- [ ] **Step 2: `components/GoogleAnalytics.tsx` erstellen**

```tsx
"use client";
/**
 * Google Analytics 4 mit Consent Mode v2 (advanced).
 *
 * - Default: alle Consent-Signale "denied" → GA sendet cookielose Pings, auch
 *   ohne Einwilligung (bewusste Risiko-Entscheidung, siehe Spec 2026-06-23).
 * - Nach Einwilligung ("Alle akzeptieren") → consent update "granted".
 * - Pageviews bei jedem Routenwechsel (App Router); interne Seiten ausgenommen.
 * - No-op, wenn NEXT_PUBLIC_GA_ID fehlt.
 */
import Script from "next/script";
import { usePathname } from "next/navigation";
import { useEffect, useRef } from "react";
import { isInternalRoute } from "@/lib/tracking-routes";
import { getConsent, subscribeConsent } from "@/lib/consent";

const GA_ID = process.env.NEXT_PUBLIC_GA_ID;

declare global {
  interface Window {
    dataLayer: unknown[];
    gtag: (...args: unknown[]) => void;
  }
}

const GRANTED = {
  ad_storage: "granted",
  analytics_storage: "granted",
  ad_user_data: "granted",
  ad_personalization: "granted",
} as const;

export default function GoogleAnalytics() {
  const pathname = usePathname();
  const ready = useRef(false);

  // 1) Bootstrap: dataLayer + Consent-Default (denied) + config. Genau einmal,
  //    VOR den Pageview-Effekten (Effekte laufen in Deklarationsreihenfolge).
  useEffect(() => {
    if (!GA_ID || ready.current) return;
    window.dataLayer = window.dataLayer || [];
    function gtag() {
      // eslint-disable-next-line prefer-rest-params
      window.dataLayer.push(arguments);
    }
    window.gtag = window.gtag || (gtag as Window["gtag"]);
    window.gtag("consent", "default", {
      ad_storage: "denied",
      analytics_storage: "denied",
      ad_user_data: "denied",
      ad_personalization: "denied",
    });
    window.gtag("js", new Date());
    window.gtag("config", GA_ID, { send_page_view: false });
    if (getConsent() === "accept") {
      window.gtag("consent", "update", GRANTED);
    }
    ready.current = true;
  }, []);

  // 2) Nachträgliche Einwilligung in dieser Session.
  useEffect(() => {
    if (!GA_ID) return;
    return subscribeConsent((d) => {
      if (d === "accept" && typeof window.gtag === "function") {
        window.gtag("consent", "update", GRANTED);
      }
    });
  }, []);

  // 3) Pageview pro Routenwechsel (inkl. erstem) — interne Seiten ausgenommen.
  useEffect(() => {
    if (!GA_ID || isInternalRoute(pathname)) return;
    if (typeof window.gtag !== "function") return;
    window.gtag("event", "page_view", {
      page_path: pathname,
      page_location: window.location.href,
      page_title: document.title,
    });
  }, [pathname]);

  if (!GA_ID) return null;

  return (
    <Script
      id="ga4-src"
      strategy="afterInteractive"
      src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}
    />
  );
}
```

- [ ] **Step 3: In `app/layout.tsx` importieren**

Nach den bestehenden Imports (bei den anderen Komponenten-Imports, z. B. nach Zeile 5 `import ClarityAnalytics ...`) ergänzen:

```tsx
import GoogleAnalytics from "@/components/GoogleAnalytics";
```

- [ ] **Step 4: In `app/layout.tsx` mounten**

Direkt vor `<PostHogProvider>{children}</PostHogProvider>` (aktuell Zeile ~185) einfügen:

```tsx
        <GoogleAnalytics />
```

- [ ] **Step 5: Build + manuelle Prüfung**

Run: `npm run build && npm run dev`
Browser (Inkognito, vor jeder Cookie-Entscheidung):
- DevTools → Network: Request an `googletagmanager.com/gtag/js` lädt.
- DevTools → Application → Cookies: **kein** `_ga`-Cookie gesetzt.
- DevTools → Network → `collect`-Request enthält `gcs=G100` (Consent denied) → cookieless Ping bestätigt.

Expected: GA lädt, aber cookieless; keine `_ga`-Cookies.

- [ ] **Step 6: Commit**

```bash
git add components/GoogleAnalytics.tsx app/layout.tsx .env.local
git commit -m "feat: GA4 mit Consent Mode v2 (cookieless default)"
```

---

### Task 3: CookieBanner scharfschalten + Widerruf-Link

**Files:**
- Modify: `components/CookieBanner.tsx`
- Create: `components/CookieSettingsLink.tsx`
- Modify: `components/sections/Footer.tsx` (Link einfügen)

**Interfaces:**
- Consumes: `getConsent`, `setConsent`, `subscribeOpenSettings` (Task 1)
- Produces: `default export CookieSettingsLink({ className }: { className?: string })`

- [ ] **Step 1: `components/CookieBanner.tsx` umbauen**

Die direkten `localStorage`-Zugriffe durch das Consent-Modul ersetzen. Imports oben ergänzen:

```tsx
import { getConsent, setConsent, subscribeOpenSettings } from "@/lib/consent";
```

Den `STORAGE_KEY`-Konstanten-Block (Zeile 8) entfernen — der Key lebt jetzt in `lib/consent.ts`.

Den ersten `useEffect` (Sichtbarkeit beim ersten Besuch) ersetzen durch:

```tsx
  useEffect(() => {
    if (hidden) return;
    if (getConsent() === null) setVisible(true);
  }, [hidden]);

  // Erneutes Einblenden bei Klick auf "Cookie-Einstellungen" (Widerruf).
  useEffect(() => {
    if (hidden) return;
    return subscribeOpenSettings(() => setVisible(true));
  }, [hidden]);
```

Die `persist`-Funktion ersetzen durch:

```tsx
  function persist(decision: "accept" | "decline") {
    setConsent(decision);
    setVisible(false);
  }
```

(Die JSX-Buttons rufen `persist("decline")` / `persist("accept")` unverändert.)

- [ ] **Step 2: `components/CookieSettingsLink.tsx` erstellen**

```tsx
"use client";
/**
 * Footer-/Datenschutz-Link zum erneuten Öffnen des Cookie-Banners (Widerruf).
 * DSGVO: Einwilligung muss jederzeit so einfach widerrufbar sein wie erteilt.
 */
import { openCookieSettings } from "@/lib/consent";

export default function CookieSettingsLink({ className }: { className?: string }) {
  return (
    <button type="button" onClick={openCookieSettings} className={className}>
      Cookie-Einstellungen
    </button>
  );
}
```

- [ ] **Step 3: Link in `components/sections/Footer.tsx` einfügen**

Import oben in der Datei ergänzen:

```tsx
import CookieSettingsLink from "@/components/CookieSettingsLink";
```

Direkt nach dem Datenschutz-`<li>` (aktuell Zeile 118) einfügen:

```tsx
              <li><CookieSettingsLink className="transition hover:text-[var(--text)]" /></li>
```

- [ ] **Step 4: Build + manuelle Prüfung**

Run: `npm run build && npm run dev`
Browser (Inkognito):
- Banner erscheint beim ersten Besuch. „Alle akzeptieren" klicken → Banner verschwindet, `wsm-cookie-consent` in localStorage = `accept`.
- Reload → Banner erscheint NICHT mehr.
- Footer → „Cookie-Einstellungen" klicken → Banner erscheint erneut.

Expected: Banner-Logik reagiert auf das zentrale Modul; Widerruf funktioniert.

- [ ] **Step 5: Commit**

```bash
git add components/CookieBanner.tsx components/CookieSettingsLink.tsx components/sections/Footer.tsx
git commit -m "feat: Cookie-Banner an Consent-Modul + Widerruf-Link"
```

---

### Task 4: PostHog & Clarity an Consent koppeln

**Files:**
- Modify: `components/PostHogProvider.tsx`
- Modify: `components/ClarityAnalytics.tsx`
- Modify: `components/GlobalOverlays.tsx` (DRY: Routen-Liste importieren)

**Interfaces:**
- Consumes: `getConsent`, `subscribeConsent` (Task 1), `isInternalRoute`/`BARE_PREFIXES` (Task 1)

- [ ] **Step 1: `components/PostHogProvider.tsx` — Init an Consent koppeln**

Imports oben ergänzen:

```tsx
import { getConsent, subscribeConsent } from "@/lib/consent";
import { isInternalRoute } from "@/lib/tracking-routes";
```

Die lokale `BARE_PREFIXES`-Konstante (Zeilen 19–26) und die lokale `isInternal`-Funktion (Zeilen 28–30) entfernen — stattdessen das importierte `isInternalRoute` verwenden. Alle Aufrufe `isInternal(pathname)` durch `isInternalRoute(pathname)` ersetzen.

Den Init-`useEffect` (aktuell Zeilen 36–49) ersetzen durch eine consent-gegatete Variante:

```tsx
  // Init genau einmal — nur auf öffentlichen Seiten UND nach Einwilligung.
  useEffect(() => {
    if (isInternalRoute(pathname)) return;

    const initIfAllowed = () => {
      if (getConsent() !== "accept") return;
      const key = process.env.NEXT_PUBLIC_POSTHOG_KEY;
      if (!key || posthog.__loaded) return;
      posthog.init(key, {
        api_host: "/ingest",
        ui_host: process.env.NEXT_PUBLIC_POSTHOG_HOST,
        person_profiles: "identified_only",
        capture_pageview: false, // App Router → manuell (siehe unten)
        capture_pageleave: true,
        disable_session_recording: true, // Clarity übernimmt Heatmaps/Replays
      });
      posthog.capture("$pageview"); // erster Pageview direkt nach späterem Opt-in
    };

    initIfAllowed();
    return subscribeConsent((d) => {
      if (d === "accept") initIfAllowed();
    });
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [pathname]);
```

Die beiden folgenden Effekte (Pageview bei Routenwechsel, zentrale Klick-/Scroll-Events) bleiben unverändert — sie sind durch `if (!posthog.__loaded) return;` bereits abgesichert und feuern erst nach Init.

- [ ] **Step 2: `components/ClarityAnalytics.tsx` — erst nach Consent laden**

Komplett ersetzen durch eine consent-gegatete Client-Komponente:

```tsx
"use client";
/**
 * Microsoft Clarity (Heatmaps/Replays) — lädt erst nach Einwilligung.
 */
import Script from "next/script";
import { useEffect, useState } from "react";
import { getConsent, subscribeConsent } from "@/lib/consent";

const CLARITY_PROJECT_ID = "wwyiou5vrl";

export default function ClarityAnalytics() {
  const [allowed, setAllowed] = useState(false);

  useEffect(() => {
    if (getConsent() === "accept") setAllowed(true);
    return subscribeConsent((d) => {
      if (d === "accept") setAllowed(true);
    });
  }, []);

  if (!allowed) return null;

  return (
    <Script id="ms-clarity" strategy="afterInteractive">
      {`
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "${CLARITY_PROJECT_ID}");
      `}
    </Script>
  );
}
```

- [ ] **Step 3: `components/GlobalOverlays.tsx` — Routen-Liste teilen (DRY)**

Die lokale `BARE_PREFIXES`-Konstante (Zeilen 19–26) entfernen und stattdessen importieren:

```tsx
import { isInternalRoute } from "@/lib/tracking-routes";
```

Die Bedingung in Zeile 30 ersetzen:

```tsx
  if (isInternalRoute(pathname)) return null;
```

- [ ] **Step 4: Build + manuelle Prüfung**

Run: `npm run build && npm run dev`
Browser (Inkognito):
- Vor Einwilligung: DevTools → Network: **kein** Request an `clarity.ms` und **kein** PostHog-Request an `/ingest`.
- „Alle akzeptieren" klicken: Clarity- und PostHog-Requests starten ohne Reload.
- Reload nach „Nur notwendige" (in frischem Inkognito): weiterhin keine Clarity-/PostHog-Requests.

Expected: PostHog/Clarity laden ausschließlich nach Einwilligung.

- [ ] **Step 5: Commit**

```bash
git add components/PostHogProvider.tsx components/ClarityAnalytics.tsx components/GlobalOverlays.tsx
git commit -m "refactor: PostHog & Clarity erst nach Einwilligung laden"
```

---

### Task 5: Datenschutzerklärung — GA4-Abschnitt (Entwurf)

**Files:**
- Modify: `app/datenschutz/page.tsx`

**Interfaces:** keine (reiner Inhalt)

- [ ] **Step 1: Bestehende Seite lesen**

Run: `sed -n '1,80p' app/datenschutz/page.tsx`
Ziel: das vorhandene Abschnitts-Markup (Überschrift + Absatz) erkennen, um den neuen Abschnitt stil-konsistent einzufügen.

- [ ] **Step 2: GA4-Abschnitt einfügen**

Im Stil der bestehenden Abschnitte (gleiche Heading-/Absatz-Klassen verwenden) nach dem bestehenden Analyse-/Cookie-Abschnitt einfügen. Inhalt (Entwurf):

```
Überschrift: Google Analytics 4

Diese Website nutzt Google Analytics 4, einen Webanalysedienst der Google
Ireland Limited (Gordon House, Barrow Street, Dublin 4, Irland). Google
verarbeitet die Daten ggf. auch auf Servern der Google LLC in den USA.

Zweck: Analyse des Nutzungsverhaltens zur Verbesserung unseres Angebots
(Reichweiten- und Performance-Messung).

Rechtsgrundlage: Soweit Sie über unseren Cookie-Banner eingewilligt haben,
erfolgt die Verarbeitung auf Grundlage Ihrer Einwilligung (Art. 6 Abs. 1
lit. a DSGVO i. V. m. § 25 Abs. 1 TDDDG). Ohne Einwilligung wird Google
Analytics im „Consent Mode" ohne Cookies und ohne dauerhafte Kennungen
betrieben; dabei werden anonymisierte, aggregierte Signale übertragen.

Datenübermittlung in die USA: Google ist nach dem EU-US Data Privacy
Framework zertifiziert. Es kann nicht ausgeschlossen werden, dass US-Behörden
Zugriff auf übermittelte Daten nehmen.

Widerruf: Sie können Ihre Einwilligung jederzeit über den Link
„Cookie-Einstellungen" im Footer mit Wirkung für die Zukunft widerrufen.

Weitere Informationen: Datenschutzerklärung von Google unter
https://policies.google.com/privacy.
```

> ⚠️ Dies ist ein Entwurf, keine Rechtsberatung. Vor Live-Schaltung von DPO/Anwalt prüfen lassen (siehe Spec).

- [ ] **Step 3: Build prüfen**

Run: `npm run build`
Expected: Seite baut fehlerfrei; neuer Abschnitt sichtbar unter `/datenschutz`.

- [ ] **Step 4: Commit**

```bash
git add app/datenschutz/page.tsx
git commit -m "docs: Datenschutz-Abschnitt zu Google Analytics 4 (Entwurf)"
```

---

## Nach dem Plan: offene Punkte (außerhalb Scope)

1. **Vercel-Env:** `NEXT_PUBLIC_GA_ID=G-Z8TF8WM9MF` in Vercel (Production + Preview) hinterlegen — sonst läuft GA4 nur lokal. Durch Albert oder per `vercel env add` nach Rückfrage.
2. **Datenschutz-Freigabe:** Entwurf aus Task 5 rechtlich prüfen lassen.
3. **GA4 ↔ Search Console / Google Ads** verknüpfen, sobald GA4 Daten sammelt (eigener Schritt).
4. **seenpilot-Script** (`app/layout.tsx`, `beforeInteractive`) lädt ebenfalls ohne Consent — gleiche Abmahn-Klasse wie PostHog/Clarity. Sollte in einem Folge-Task ebenfalls an das Consent-Modul gekoppelt werden.

## Self-Review (durchgeführt)

- **Spec-Abdeckung:** `lib/consent.ts` (T1) ✔, GA4 + Consent Mode v2 (T2) ✔, Banner scharf + Widerruf (T3) ✔, PostHog/Clarity gegatet (T4) ✔, Datenschutz-Text (T5) ✔, DRY-Routenliste (T1/T4) ✔, Env-Variable (T2) ✔.
- **Platzhalter:** keine offenen TODOs; alle Code-Blöcke vollständig.
- **Typ-Konsistenz:** `ConsentDecision`, `getConsent`/`setConsent`/`subscribeConsent`/`openCookieSettings`/`subscribeOpenSettings`, `isInternalRoute`/`BARE_PREFIXES` über alle Tasks identisch benannt.
